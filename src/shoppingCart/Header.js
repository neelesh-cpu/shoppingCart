import React from "react";
import { FaShoppingCart } from "react-icons/fa";

const Header = () => {
  return (
    <header className="header">
      <h1>header added in master branch</h1>
      <h2>header need to be removed</h2>
    </header>
  );
};

export default Header;
